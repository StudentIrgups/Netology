* 1. Будет запущен файл compose.yml, так как второе имя файла является устаревшим и если даже выполнить команду docker-compose up в директории, где два этих файла, то docker предпотчтёт именно compose.yml;
